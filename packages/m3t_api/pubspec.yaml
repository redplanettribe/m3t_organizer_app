name: m3t_api
description: "API client for the m3t attendee backend."
version: 0.1.0
publish_to: "none"

environment:
  sdk: ^3.10.8

dependencies:
  http: ^1.2.2
  json_annotation: ^4.11.0
  equatable: ^2.0.8

dev_dependencies:
  build_runner: ^2.11.1
  json_serializable: ^6.9.0
  test: ^1.30.0

