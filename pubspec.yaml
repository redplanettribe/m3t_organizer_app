name: m3t_organizer
description: "Flutter app for m3t event attendees."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.10.0 <4.0.0'

dependencies:
  attendee_repository:
    path: packages/attendee_repository
  auth_repository:
    path: packages/auth_repository
  domain:
    path: packages/domain  
  equatable: ^2.0.8

  events_repository:
    path: packages/events_repository
  flutter:
    sdk: flutter

  flutter_bloc: ^9.1.1
  flutter_secure_storage: ^10.0.0
  go_router: ^17.1.0
  image_picker: ^1.1.2
  m3t_api:
    path: packages/m3t_api

dev_dependencies:
  bloc_test: ^10.0.0
  build_runner: ^2.11.1
  flutter_launcher_icons: ^0.14.4
  flutter_test:
    sdk: flutter

  mocktail: ^1.0.4
  very_good_analysis: ^10.1.0


# Flutter Launcher Icons - generates app icons from assets/icon
flutter_launcher_icons:
  android: "launcher_icon"
  ios: true
  remove_alpha_ios: true # Required by Apple (no transparent backgrounds)
  image_path: "assets/icon/icon_master.png" # 1024x1024 source
  adaptive_icon_background: "#FFFFFF"
  adaptive_icon_foreground: "assets/icon/icon_foreground.png"


flutter:

  uses-material-design: true

  assets:
    - assets/icon/
